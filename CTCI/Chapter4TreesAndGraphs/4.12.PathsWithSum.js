/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} root
 * @param {number} sum
 * @return {boolean}
 */

/*
Brute-Force Approach : Simplify the problem so that we

 */
var pathsWithSum = function(root, sum) {
  //DFS with
};
